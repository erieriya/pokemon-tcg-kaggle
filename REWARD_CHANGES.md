# PPO自己対戦の報酬設計 変更まとめ

> `agent/train_ppo.py` の報酬・評価ロジックに対する変更点の記録。
> 関連: [research.md](research.md) 6章「強化学習アプローチの考察」

## 発生していた問題

2000エピソード前後の学習で、**攻撃せずデッキアウトで勝つ**挙動に収束した。

### 原因

1. **勝因を区別しない報酬**: KOでサイドを取り切った勝ちもデッキアウトの勝ちも同じ+1.0だった。攻撃は多段階の準備（進化・エネルギー付け等）が必要でリスクもあるのに対し、パスし続けるだけのデッキアウト待ちは低リスクで再現性が高く、PPOの勾配がそちらに偏って強化されやすい。
2. **先攻後攻の固定**: `cg`エンジンの`battle_start(deck0, deck1)`は常に第1引数側（`player_idx 0`）を先攻にする（コイントスではなく決定的、20/20で確認済み）。先攻はドローを1回スキップするため、何もしなくても後攻が先にデッキアウトしやすい構造的アドバンテージがある。
3. **評価関数のバイアス**: `evaluate()`/`play_eval_game()`が学習中ネットを常に`player_idx 0`（＝常に先攻）に固定していたため、ランダム相手への勝率がこの先攻アドバンテージだけで見かけ上高く出ていた可能性がある。

## 実施した変更（`agent/train_ppo.py`）

### 1. 評価時の先攻後攻ランダム化

`play_eval_game`で学習中ネットの`player_idx`を毎試合`random.randint(0, 1)`で決定。`evaluate()`の勝敗判定もこれに合わせて修正。自己対戦の学習ループ自体は両シートとも同じ共有ネットなので変更不要（先攻後攻どちらの手番もエピソード内で必ず経験する）。

### 2. 勝因ごとの勝利報酬

試合終了ログ（`logs`内の`type==23`の`RESULT`ログ、`reason`フィールド）から勝因を判定。

| reason | 意味 | 勝利報酬 |
|---|---|---|
| 1 | サイド0枚（通常のKO決着） | 1.0固定 |
| 2 | デッキアウト | `--deckout_win_reward`（デフォルト**0.5**） |
| 3 | 場のポケモンが0（全滅勝ち） | 1.0固定 |
| 4 | カード効果 | 1.0固定 |

### 3. サイド取得の中間報酬（1枚ずつ・終盤ほど強く）

サイドを取った瞬間、何枚目に取ったかに応じて`--prize_rewards`（カンマ区切りリスト）から報酬を加算。勝敗報酬（±1.0前後）より弱い値にして、サイド取り＝攻撃的プレイを後押しする。

- デフォルト: `0.05,0.05,0.08,0.1,0.15,0.25`（1枚目→6枚目、終盤ほど大きくしてフィニッシュを後押し）

### 4. 敗因ごとの敗北罰則

通常のKO負けより、消極的・雑な負け方の罰則を強化。

| reason | 意味 | 敗北報酬 |
|---|---|---|
| 1, 4 | 通常のKO負け | -1.0固定 |
| 2 | 自分がデッキアウト | `--deckout_loss_reward`（デフォルト**-1.5**） |
| 3 | 自分の場のポケモンが0（全滅） | `--wipeout_loss_reward`（デフォルト**-1.5**） |

### 5. 全報酬パラメータをCLIから設定可能に

```
uv run python agent/train_ppo.py \
  --prize_rewards "0.05,0.05,0.08,0.1,0.15,0.25" \
  --deckout_win_reward 0.5 \
  --deckout_loss_reward -1.5 \
  --wipeout_loss_reward -1.5
```

### 6. 複数条件の同時学習スクリプト（`run_experiments.sh`）

異なる報酬設定を並行してバックグラウンドで学習できる。各設定は`agent/models/<名前>/`にチェックポイント、`agent/logs/<名前>.log`にログを出力。

```
./run_experiments.sh [episodes]   # デフォルト2000
```

設定を変えたい場合はスクリプト内`EXPERIMENTS`配列の各行を編集する:

```
"名前|サイド1〜6枚目の中間報酬(カンマ区切り)|デッキアウト勝ち報酬|デッキアウト負け罰則|全滅負け罰則"
```

### 診断用ログ

`deckout_win_rate(100)`を学習ログに追加（直近100試合のデッキアウト勝ち比率）。これが下がっていくかどうかで、今回の対策が効いているかを確認できる。

## 今後の検討事項（未実装）

- ダメージを与えたことに対する中間報酬（research.md 6章の案。現状は未実装）
- 報酬アニーリング（学習後期は中間報酬を減らし、純粋な勝敗報酬に絞る）
- デッキを固定から可変にする（research.md Phase3）
