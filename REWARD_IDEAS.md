# 報酬再設計アイデア（未実装・後回し）

> Kaggle Notebookの2つのMega Lucarioエージェントの価値関数を読んで出てきた、
> `agent/train_ppo.py` の報酬設計に対する改善アイデアのメモ。
> 現時点では実装しない（ユーザー判断で後回し）。実装する場合は
> [REWARD_CHANGES.md](REWARD_CHANGES.md) と同じ形式で変更点を追記する。
>
> 参照notebook:
> - https://www.kaggle.com/code/nursrijan/pokemon-ai-battle-agent-mega-lucario
> - https://www.kaggle.com/code/pilkwang/pokemon-tcg-lucario-v2-strategy-baseline

## 元notebookの価値関数（前向き探索のリーフ評価用、RL報酬ではない）

両notebookとも実際の対戦方策はルールベースのスコアリングで、ニューラルネットは
使っていない。ただし任意機能の前向き探索（このリポジトリのcgライブラリでは
search_begin の引数仕様が違うため動作しない）のリーフ評価に、ヒューリスティックな
状態価値関数を使っている。これは「報酬」そのものではないが、特徴量の重み付けの
参考になる。

### v1: `evaluate_state`

```python
val = (op.prize残り数 - me.prize残り数) * 10000.0   # プライズ差が支配項
val += 各ポケモンのエネルギー数 * 120.0
val += 主力ポケモン(Mega Lucario ex / Hariyama)が場にいるボーナス
val += 自分のアクティブHP * 1.0
val -= 相手のアクティブHP * 1.5
val += 手札枚数 * 5.0
```

### v2: `_board_value`（よりwin-aligned）

```python
if 終局: return ±1_000_000          # 勝敗確定を最優先
val = 10000 * (op.prize残り - me.prize残り)
val += 2.0 * 相手アクティブへの累積ダメージ
val += 800 * 相手アクティブを倒した時のプライズ価値
val -= 1.5 * 自分アクティブへの累積ダメージ
val -= 600 * 自分アクティブを失った時のプライズ価値
val += 30 * 各ポケモンのエネルギー数
val += 300 * 攻撃可能状態のポケモン数
val += 20 * 手札枚数
if デッキ残り <= 4: val -= 500 * (5 - デッキ残り)   # デッキアウト負けリスク
```

v2はv1より「終局状態を明示的に大きな値で扱う」「ダメージ量とKOリスクを分離する」
「デッキアウトという別の敗北条件を組み込む」点で洗練されている。

## 採用を検討する場合の方針

1. **コア報酬は終局の勝敗のみ**にする（現状の実装と同じ方向性）。
2. **密な中間報酬はプライズ差の変化量を中心にする**。プライズ差は勝利条件と直結するため、
   `Δ(自分の取得プライズ - 相手の取得プライズ)` を毎ターンの報酬に加えるのは
   potential-based reward shaping（Ng et al. 1999）の形になり、最適方策を歪めずに
   学習を加速できる。現状の `prize_rewards`（自分が取った時だけ加点）を、相手が
   取った時に減点する項に拡張する案がここから来ている。
3. **エネルギー数・手札枚数・ready_attackers的な項は報酬に直接混ぜない**。
   係数が大きいと「攻撃せずエネルギーを溜め込む」「手札を吐かず抱え込む」ような
   退化方策を誘発しやすい（実際に現状のreward設計でも「デッキアウト待ち」という
   類似の退化が`REWARD_CHANGES.md`で報告されている）。代わりに:
   - 観測の特徴量としてエージェントに与える（すでに`encode_state.py`でエネルギー数等を
     渡しているはずなので重複しないか確認する）、または
   - 価値関数(critic)の事前学習（warm-start）にのみ使う
   といった使い方が安全。
4. **デッキアウト(山札切れ)リスクのペナルティ**は実際の敗北条件と直結しているため、
   報酬に組み込んでも問題ない。現状の `deckout_loss_reward` と方向性は同じだが、
   v2のように「残り山札が少ないほど線形に罰則を強める」形に滑らかにする余地はある。
5. **相手へのダメージ量に応じた中間報酬**（research.md 6章で既に検討済み・未実装の項目）も
   このタイミングで一緒に検討するとよい。KOに至らないダメージ蓄積をわずかに評価することで、
   攻撃の機会コストを下げられる可能性がある。ただし「ダメカン乗せに固執する」退化を
   誘発しないよう、勝敗報酬よりかなり弱い係数にし、後期は0にアニーリングする。

## 進め方の提案（実装するときに）

- 既存の `run_experiments.sh` のパターンに乗せて、現行のprize_rewards設計と
  「プライズ差を相手減点込みにした設計」をA/Bする。
- 報酬を変える前後で `deckout_win_rate(100)` や新設の対戦相手別勝率
  （`agent/train_ppo.py` の `opponent_win_history`、本PRで追加）が
  どう変わるかを比較すると、退化方策が再発していないか確認しやすい。
